#include <stdio.h>

int main()
{
    int i;
    char c;
    float f; 
    double d;
    printf("Size of int = %ld\n", sizeof(i));
    printf("Size of char = %ld\n", sizeof(c));
    printf("Size of float = %ld\n", sizeof(f));
    printf("Size of double = %ld\n", sizeof(d));
    return 0;
}

